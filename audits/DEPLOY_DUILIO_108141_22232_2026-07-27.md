# Deploy — 2 pendências do Duílio (NF 108141 + NF 22232) — 2026-07-27

Autor: Matheus. Fecha o ciclo das 2 pendências reportadas pelo Duílio (Key
Accounts) em 27/07 11:41-11:43.

## Pendência 1 — NF 108141: "Já tem tratativa? Buscar" dá erro + resposta não aparece

**Sintoma:** cliente respondeu por e-mail, o retorno não aparecia no Cockpit
(sem lançar a oc 21); ao clicar "Já tem tratativa? Buscar" dava erro e "não
puxava".

**Diagnóstico (2 causas independentes, provadas):**
1. **Bug de UI (front):** o `switch` do `BotaoBuscarTratativa` só tratava
   sugerido/nenhum_candidato/sem_*/card_inexistente e jogava
   `adotado`/`ja_decidido`/`card_terminal` no `default: toast.error("Resultado
   inesperado")`. O scan do 108141 resultou `adotado` (thread já vinculada,
   decidida 11:12), mas a materialização da tratativa atrasou ~7h e o botão
   seguiu visível (só some com `total_tratativas>0`) → a re-checagem devolvia
   `ja_decidido` → **toast de ERRO num scan que teve SUCESSO**. Evidência: 0
   erros de scan em 27/07 (`email_preexistente_scan`) — NÃO era a função falhando.
2. **Bug de latência (backend/ops):** as ~7h entre captar e o card exibir. Raiz
   comum da **causa-2 do gmail-poll** (backlog do poller + filas congestionadas).
   Fix já mergeado (memória de avaliação, flag OFF), **pendente de deploy do
   `gmail-poll-inbox` + flip da flag** — ver [[project-gmail-poll-causa2-memo-avaliacao]].

**Fix desta entrega (só o bug 1):** helper puro testado
`apps/cockpit-web/src/lib/scan-tratativa-resultado.ts` — os códigos benignos
viram INFO + refresh, nunca erro. Guard: `scan-tratativa-resultado.test.ts` (11).

## Pendência 2 — NF 22232: não consegue lançar oc 44 (última oc = 31)

**Diagnóstico (raiz confirmada):** **não existe card** (0 em cards/inbox/outbound).
oc 31 ("Aguardando agendamento") está fora do escopo de relacionamento → o
Cockpit não fabrica card → sem superfície pra lançar a oc 44 ("Retorno de carga").

**Fix (opção 1 do Caio, escopo pontual):** `criar-card-manual` passa a permitir
criação com oc fora de relacionamento **SÓ com justificativa explícita** do
operador (min 10 chars). Decisão pura `_shared/gate-criacao-card-manual.ts`;
card nasce com `fora_de_padrao=true` + motivo auditado em `card_events`
(CardCriadoManualmente) e `agent_state`; front `ModalCriarCard` mostra textarea +
botão "Criar mesmo assim". Guard: `gate-criacao-card-manual.test.ts` (5) + INV-059.

**Blast radius verificado:** card manual com oc fora de relacionamento é INERTE
ao sync-bastao (todos os passes/sweeps são escopados a ocs de relacionamento ou
ocs com regra). Não viola INV-006 (oc≠54) nem INV-019 (state≠AGUARDANDO_CLIENTE).

## Commits / merges (base: `be74172`)

- `322bd32` fix(front): botão Buscar Tratativa não erra em scan bem-sucedido — merge `3aeb09a`
- `7bbc4e3` fix(card-manual): criar card com oc fora de relacionamento COM justificativa — merge `06bb2fd`

`master == origin/master == 06bb2fd`. Cada merge foi `--no-ff` e aditivo (só o
próprio commit); trabalho anterior (incidente 26/07 incluso) preservado.

## Verificação

- `deno check` criar-card-manual + gate: **0 erros** (delta 0).
- Front `typecheck` (tsconfig.app.json) + `build` (vite): **0**.
- Testes: front **86/86**; deno helpers **14/14** (gate 5 + gmail-poll-batch 9).
- Security advisors: **0 ERRORs** (110 lints; baseline).
- `/verify-cockpit` (subset sem `psql`): INV-029 PASS, INV-059 PASS, INV-049 PASS,
  Fase 1/6 PASS. Checks de DB = SKIP (sem `psql` no ambiente).

## Estado de deploy

- **Backend `criar-card-manual` (P2):** DEPLOYADO — via `npx supabase functions
  deploy` (passou pelo deploy-gate). Confirmado na Management API: **version 18,
  status ACTIVE, updated_at 2026-07-27T23:06:49Z**.
- **Front (P1 + P2):** auto-deploy pelo projeto Vercel NOVO do Caio (conectado ao
  `caio-maker2020/cockpit-v2` após a transferência do repo). O merge no `master`
  é o gatilho. Pendente: Caio confirmar no dashboard dele que o build do
  `06bb2fd` ficou READY. O projeto antigo `cockpitsalai` (conta do Matheus) ficou
  desconectado na transferência — ignorar.

## Follow-ups

- Deploy da **causa-2 do gmail-poll** (`gmail-poll-inbox` + flip da flag
  `gmail_poll_memo_avaliacao_ativo`) resolve a latência de captura da Pendência 1.
- Colisão de número de migration a limpar: `2026-07-23_306_*` existe em duas
  variantes (gmail_poll_eval_memo + cron_arbitro_desfecho) — só higiene de nome.
